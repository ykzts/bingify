# ==========================================
# Supabase ローカル開発設定
# ==========================================
# `pnpm exec supabase status -o env` で取得した値を設定してください
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase Auth Hook Secret
# 認証メール送信用のWebhook署名検証に使用
# Magic Linkログインを有効にするには、この設定が必須です
# 
# 設定手順:
# 1. ランダムなシークレットを生成: openssl rand -base64 32
# 2. 「v1,whsec_<生成された値>」の形式で設定
# 3. Supabaseを再起動: pnpm local:stop && pnpm local:setup
# 
# 注意: この値はSupabaseコンテナとNext.jsアプリの両方で使用されます
# supabase/config.toml の [auth.hook.send_email] セクションで参照されます
# 
# 例: SEND_EMAIL_HOOK_SECRETS=v1,whsec_abcdefghijklmnopqrstuvwxyz1234567890
SEND_EMAIL_HOOK_SECRETS=

# ==========================================
# アプリケーション設定
# ==========================================
# ローカル開発: http://localhost:3000
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ベータ版バナーの表示制御（正式リリース時にfalseに設定）
NEXT_PUBLIC_SHOW_BETA_BANNER="true"

# ==========================================
# 機能フラグとアクセス制御
# ==========================================
# Basic認証（公開前制限など）
ENABLE_BASIC_AUTH="false"
BASIC_AUTH_USER=""
BASIC_AUTH_PASSWORD=""

# ==========================================
# 定期ジョブ
# ==========================================
# Vercel Cronジョブの認証用シークレット
CRON_SECRET=""

# ==========================================
# OAuth プロバイダー認証情報（ローカル開発用オプション）
# ==========================================
# プロバイダーに登録してローカルテスト用に認証情報を追加します。
# README.md の「認証について」セクションを参照してください。

# Google OAuth
# https://console.cloud.google.com/ に登録
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# Twitch OAuth
# https://dev.twitch.tv/console/apps に登録
SUPABASE_AUTH_EXTERNAL_TWITCH_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_TWITCH_SECRET=

# ==========================================
# SMTP設定（お問い合わせフォーム）
# ==========================================
# SMTPサーバー接続設定
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user"
SMTP_PASS="password"
SMTP_SECURE="false"
MAIL_FROM="noreply@your-domain.com"
# 送信先: profiles テーブルの role = 'admin' のユーザーに自動送信されます

# ==========================================
# Cloudflare Turnstile（オプション）
# ==========================================
# Bot対策用。空にするとスパム保護を無効化
# https://dash.cloudflare.com/ で取得
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# ==========================================
# Google Analytics（オプション）
# ==========================================
# GA4測定ID。空にするとアクセス解析を無効化
# https://analytics.google.com/ で取得
NEXT_PUBLIC_GA_MEASUREMENT_ID=""
