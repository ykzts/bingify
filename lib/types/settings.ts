export interface SystemFeatures {
  gatekeeper: {
    email: {
      enabled: boolean;
    };
    twitch: {
      enabled: boolean;
    };
    youtube: {
      enabled: boolean;
    };
  };
  // Other feature flags can be added here
}
